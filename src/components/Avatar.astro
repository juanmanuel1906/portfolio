<div class="flex-shrink-0 relative group neon-orbit-box">
    <img
        src="https://portfolio-nine-lovat-35.vercel.app/assets/img/avatar.webp"
        alt="Juan Manuel Yate Méndez"
        class="size-52 md:size-64 lg:size-80 rounded-full object-cover relative"
        fetchpriority="high"
        loading="eager" 
        decoding="async"
    />
</div>
<style>
  /* =========================================
     DEFINICIÓN DE COLORES POR TEMA
     ========================================= */
  :root {
    /* Usamos azules más profundos y saturados para que resalten sobre blanco */
    --orbit-ring-base: rgba(37, 99, 235, 0.15);
    --orbit-glow-color: rgba(30, 64, 175, 1);
    --orbit-glow-spread: rgba(59, 130, 246, 0.6);
  }

  .dark {
    /* Usamos azules claros y brillantes que "encienden" el fondo negro */
    --orbit-ring-base: rgba(96, 165, 250, 0.1);
    --orbit-glow-color: rgba(96, 165, 250, 1);
    --orbit-glow-spread: rgba(96, 165, 250, 0.8);
  }


  /* =========================================
     ANIMACIÓN Y ESTRUCTURA
     ========================================= */
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .neon-orbit-box {
    position: relative; 
    border-radius: 9999px;
    /* Usamos la variable para el riel base. 
       En modo claro será más oscuro y visible. */
    box-shadow: 0 0 0 3px var(--orbit-ring-base); 
    /* Opcional: un fondo sutil detrás de la imagen para dar profundidad en modo claro */
    background-color: var(--orbit-ring-base); 
  }

  /* EL PUNTO NEÓN QUE GIRA */
  .neon-orbit-box::before {
    content: '';
    position: absolute;
    inset: -6px; 
    border-radius: 9999px; 
    z-index: -1; /* Detrás de la imagen */
    
    /* 
       El punto se verá más sólido y definido sobre blanco.
    */
    background: radial-gradient(circle at top, var(--orbit-glow-color) 0%, transparent 20%, transparent 100%);
    
    /* El filtro usa un color de dispersión más saturado en modo claro 
       para crear un "halo" vibrante alrededor del punto.
    */
    filter: drop-shadow(0 0 12px var(--orbit-glow-spread));
    
    animation: spin 4s linear infinite;
  }
</style>